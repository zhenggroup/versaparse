[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "versaparse"
version = "0.3.0"
description = "A native Python parser for VersaStudio .par files by Weiran Zheng (Guangdong Technion)"
dependencies = [
    "pandas",
]
